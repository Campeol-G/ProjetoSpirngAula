version: "3.8"

services:
  pgadmin:
    image: dpage/pgadmin4
    container_name: meu-pgadmin
    network_mode: host
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_EMAIL}
    restart: always
